const userName = "JD";

console.log(userName);

// THE LIB ALLOWS US TO KNOW WHAT TYPESCRIPT CAN USE
// DEFAULT IS SET IF ITS NOT SELECTED - all es6 ways

//SOURCE MAP in TS.Config allows use of debugging typescript on console

// folder structure will keep consistent to dist folder
